{
  "name": "czy-isc-na-wyklad",
  "version": "1.0.0",
  "description": "Algorytm decyzyjny pomagający zdecydować studentowi czy danego dnia należy pójść na wykład.",
  "repository": "https://github.com/KamilLelonek/czy-i--na-wyk-ad",
  "scripts": {
    "start": "npm run build && node --es_staging server.js",
    "dev-server": "live-server --entry-file=index.html",
    "build": "babel scripts.es6 --out-file scripts.js --source-maps",
    "watch": "babel scripts.es6 --out-file scripts.js --source-maps --watch",
    "postinstall" : "npm run build"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-preset-es2015": "^6.3.13",
    "live-server": "^0.8.2"
  },
  "dependencies": {
    "express": "^4.13.3"
  }
}
